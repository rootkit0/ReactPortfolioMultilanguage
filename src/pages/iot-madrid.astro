---
import IoTLanding from '@/components/IoTLanding.astro';
import { resolveLang, t } from '@/i18n';
const lang = resolveLang(Astro.request);
const city = t(lang, 'cities.madrid') || 'Madrid';
---
<IoTLanding lang={lang} city={city} slug="madrid" />
