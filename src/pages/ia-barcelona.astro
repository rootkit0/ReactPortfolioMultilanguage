---
import AILanding from '@/components/AILanding.astro';
import { resolveLang, t } from '@/i18n';
const lang = resolveLang(Astro.request);
const city = t(lang, 'cities.barcelona') || 'Barcelona';
---
<AILanding lang={lang} city={city} slug="barcelona" />
